#pragma once
#define ADD(x, y) ((x) + (y))
#define SUBTRACT(x, y) ((x) - (y))
#define MULTIPLY(x, y) ((x) * (y))
#define DIVIDE(x, y) ((y) != 0 ? ((x) / (y)) : 0)
#define POWS(x, y) (std::pow((x), (y)))
#define SQRT(x) (std::sqrt(x))
#define Height(x, y, z) ((x + y) / z)